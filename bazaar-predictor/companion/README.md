# Bazaar Predictor companion

Copy `config.example.json` to `config.json`, put your Personal Hypixel API key in it, install dependencies with `python -m pip install -r requirements.txt`, then run `python server.py`.
